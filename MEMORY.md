# GEKIAZU RUSH — Build Memory

- Reactは画面枠とHUD、Babylonは背景のWebGL描画を担当する。
- 初回ユーザー操作前はブラウザの自動再生制限に従い、音を鳴らさない。
- 生成アセットはプロジェクト内に複製せず、`/manus-storage/` の固定URLを利用する。
- 検証用の `?demo` は、ハズレ → 当たり → 大当たりを固定順序で再生する。

## 物理盤面テスト（2026-08-20）

- SWEET域（発射強度62）で通常球を約1.8秒発射し、仮想BALLが180から173へ減少、1回の図柄変動と結果表示まで進むことを確認した。
- 固定座標の釘群、左右レール、電チュー導入路、アタッカー導入路を共有し、盤面表示の釘位置と衝突位置が一致する。

## 仮想実機モードテスト（2026-08-20）

- 通常球を発射して物理始動口入賞後、抽選ID `0478` と `VIRTUAL RNG LOCKED` が表示され、固定された `0-0-0` 図柄と大当たり／アタッカー状態へ遷移することを確認した。
- 図柄表示は物理入賞後に開始し、入賞ID、列ごとの停止状態、最終結果を同一コンテキストから描画する。抽選結果を変動中に再抽選しない。

## 仮想球循環テスト（2026-08-20）

- `?stage=attacker`で右打ち・電チュー・アタッカーを開放し、HOLD TO FIREから手動で右打ち球を連続発射する検証を開始した。上皿、下皿、アウト、戻り口、下皿→上皿の移送操作を同一筐体へ表示している。
- 右打ちを約1.6秒継続した結果、上皿は180から174へ減少し、物理アウト口は0から5へ更新された。球が発射ごとに台帳から減り、物理盤面の到達先がアウト表示へ反映されることを確認した。
- 戻り口の捕捉幅を外周球道に合わせて拡張し、同じアタッカー盤面で右打ちの再試験を開始した。
- 電チュー通過後にアタッカーへ入賞しなかったRELAY誘導球も、下皿戻り口で受けるよう物理捕捉条件を修正した。
- 修正後の右打ち約2.2秒では、上皿180→173、下皿0→1、アウト0→5を確認した。アタッカー未入賞のRELAY誘導球が物理戻り口を通って下皿へ入り、残りはアウト口へ回収される。
- `TRAY TRANSFER`操作で下皿1→0、上皿173→174へ更新され、仮想BALLの下皿→上皿循環が完了することを確認した。

## 可動物理ギミックテスト（2026-08-20）

- 通常盤面でSWEET域の左打ちを約2.4秒開始し、SEAL SHUTTERとKAGURA ROTORを含む物理球道の接触・進路変化を確認中である。
- 初期配置では通常球の監視中に`ROTOR KICK`表示を観測できなかったため、左ローターを始動口前球道（42, 39.6）、右ローターを電チュー前球道（84.3, 54）へ再配置した。両者とも物理バンパーの半径と反発キックを持つ。
- 左ローターの衝突半径を5.35へ拡大した再試験では、通常球を約3.6秒発射中に`ROTOR KICK`状態が1回観測された。可動ローターが実際の物理球道に接触し、画面状態へ反映されることを確認した。
- `?stage=attacker`では、右ローターの物理接触表示が初期誘導球にも現れた。手動右打ちを約2秒継続した監視では`RIGHT ROTOR KICK`が6回観測され、電チュー前ローターが右打ち球道に継続して作用することを確認した。

## 盤面同時球数上限テスト（2026-08-20）

- 通常盤面で約7秒間の連続発射を行い、`BOARD BALLS`を30ms間隔で監視した。観測最大値は20、設定上限も20であり、物理盤面の同時球数が20を超えないことを確認した。

## 演出・音量設定モーダル（2026-08-20）

- ヘッダーの`SET`から、演出強度と音量を0〜100%・5%刻みで調整するモーダルを追加した。両スライダーはキーボード操作・個別の支援技術ラベルを備える。
- 演出強度と音量をそれぞれHomeキーで0%へ移動し、`localStorage`の`gekiazu-rush.settings.v1`が`{"effects":0,"volume":0}`となることを確認した。`初期値へ戻す`後は`{"effects":80,"volume":78}`に復帰することを確認した。
- モーダル、SET導線、LCD主宣告、盤面20球カウンターはデスクトップと375px幅のモバイル表示で確認済み。モバイルでは、設定導線をヘッダー右端へ保ち、盤面とハンドルの操作領域を阻害しない。

## スマホ操作ドック（2026-08-20）

- 375px幅では、盤面球数、LEFT／RIGHT球道、設定、発射強度、長押しFIREを固定の親指操作ドックへ集約した。通常時とATTACKER時で状態色と球道選択が正しく分離されることを確認した。
- 設定モーダルは全幅のボトムシートへ切り替え、広いスライダーつまみ、閉じる、初期値復帰、適用ボタンが同一ビューポート内に収まることを確認した。
- FIREの共有発射処理を4.2秒間保持する実地テストで、盤面球数は`20/20`へ到達し、それ以上増加しないことを再確認した。固定ドックは既存の物理発射・20球制御と同一のゲーム操作を呼び出す。
- `?stage=attacker`では、RIGHT E-TULIP球道、ATTACKER表示、盤面球数表示を確認した。本番ビルドと型チェックを完了している。

## 縦向きスマホ再設計（2026-08-21）

- 縦画面では固定ドックを撤去し、LCD → 計器 → 盤面 → FIRE → PUSH／RESET／TRAY TRANSFER の自然なスクロール順へ変更した。これにより、初期ビューポートで盤面・計器が固定操作UIに隠れない。
- 初期LCDには`PLAYFIELD ↓`の非遮蔽導線を追加し、盤面へスムーズに移動できるようにした。設定は画面全幅のボトムシートで、背後を暗転させつつ操作領域は全て視認できる。
- DOM検証で、`PLAYFIELD ↓`は`#mobile-playfield`を正しく参照し、操作ドックは盤面と同じ`pachinko-deck`内にあることを確認した。端末内の設定保存も維持されている。

## 実機縦画面に合わせた初期ビュー圧縮（2026-08-21）

- 添付の約402×874 CSSピクセル相当の縦画面で、LCDを11.55remのコンパクト状態表示へ切り替えた。初期ビュー内に上皿・下皿・保留・盤面・LEFT／RIGHT・FIRE・PUSH・RESET・TRAY TRANSFERが連続して見える。
- 縦向き専用の`portrait-oracle-compact`を導入し、待機時は「運命待機」と3図柄、ATTACKER時は「RED ACCESS」を常時表示する。設定は同サイズで全幅ボトムシートとして確認済み。
- 公式主紋をLCD／盤面の接合部へ追加し、盤面釘路の通常ガイド文言を`GATE LOCK`へ変更した。402×874の縦画面で通常・ATTACKER・設定ボトムシートを確認し、型チェックと本番ビルドを完了した。
