# GEKIAZU RUSH パチンコ基盤仕様

## 1. 因果の原則

パチンコ版のゲーム進行は、**ハンドル入力→遊技球発射→盤面移動→始動口入賞→保留→図柄変動→リーチ→図柄停止→大当たり→アタッカー→ラウンド→仮想BALL払い出し**とする。ハンドルや球が存在しない状態で図柄変動を開始してはならず、プレイヤーによる図柄停止操作は実装しない。

## 2. 状態モデル

| 項目 | 値 | 目的 |
| --- | --- | --- |
| `phase` | `standby` / `firing` / `variation` / `reach` / `reveal` / `jackpot` / `attacker` / `roundSettle` / `result` / `log` | 入力可能性と表示を一意にする。 |
| `balls` | 整数 | 発射できる仮想遊技球。現金価値を持たない。 |
| `reserve` | `VariationContext[]`、最大4件 | 始動口に入った瞬間に生成し、FIFOで消化する。 |
| `activeBalls` | `BallState[]` | 盤面を実際に移動している球。 |
| `symbols` | 3つの`DisplaySymbol` | LCDで自動変動・停止する図柄。 |
| `award` | `miss` / `smallWin` / `jackpot` / `chance` | 変動の結末。 |
| `round` | 0〜3 | 大当たり中のラウンド番号。 |
| `paidBalls` | 整数 | 現在変動・ラウンドで増えた仮想BALL。 |

## 3. 遊技球と始動口

ハンドル押下中は、仮想BALLが残っているかぎり、発射間隔に従って1球ずつ左発射レーンから出る。各球は`x`、`y`、`vx`、`vy`、`seed`を持ち、固定ピン配列との交差で横速度を微小に変化させる。球が始動口の捕捉領域へ入った場合にのみ、盤面上から削除して保留を1件追加する。

保留が4件に達した時点では、新しい始動口入賞を抽選に変換しない。これは保留灯の表示と実際の抽選待ちを必ず一致させるためである。

### 3.1 盤面物理モデル

盤面座標は横`0〜100`、縦`0〜100`の正規化空間とする。通常球の半径は`1.05`、重力は`58 units/s²`、空気・盤面摩擦はフレームごとに速度を`0.995`倍、釘衝突の反発係数は`0.56`、レール衝突の反発係数は`0.43`とする。フレーム時間は最大`12ms`ごとに分割し、球の高速通過による釘抜けを抑える。

釘は左発射から中央の寄り釘、始動口上の命釘、下部の障害釘、右打ち側の電チュー寄せ釘、アタッカー導入路へ分ける。各釘は固定座標と半径`0.8`を持つ円として扱い、球と重なった場合に接触法線方向へ押し戻し、法線速度だけを反転・減衰させる。左右外壁、左発射レール、右打ちレール、アタッカー導入路は線分壁として同じ反発解決を受ける。

始動口は中心`(50, 53)`・半径`4.1`、電チューは中心`(85, 64)`・半径`4.0`、アタッカーは中心`(50, 90)`・横幅`12`・縦幅`4.8`の捕捉窓とする。球は対象の捕捉窓へ低下方向に侵入し、対象が開放・受付可能なときだけ入賞する。右打ち球がアタッカー中に電チューを通過した場合は、物理的な右下導入レールへ移し、下部アタッカーへ実際に到達した場合のみ払い出す。

## 4. 変動コンテキスト

`VariationContext`は始動口入賞時に作成し、結果、停止図柄、予告強度、リーチ種別、大当たりラウンド数、仮想払い出し量を固定する。表示中に別の乱数で結果を上書きしてはならない。

### 4.1 仮想実機モードの抽選と図柄回転

本モードは**仮想BALL限定**であり、現金、換金、賭け、景品交換を扱わない。始動口または電チューの物理捕捉が成立した瞬間に、`0〜9999`の仮想抽選チケットを一度だけ生成する。チケットは左保留では`jackpot 0〜1049`、`win 1050〜3299`、`miss 3300〜9999`、右保留では`jackpot 0〜1399`、`win 1400〜3899`、`miss 3900〜9999`へ対応付ける。これらは本作固有の娯楽用テーブルであり、実在機のスペックを模倣・保証するものではない。

抽選チケット、停止図柄、リーチ、払い出し、ラウンド数は入賞直後に`VariationContext`へ凍結する。図柄の自動回転は物理入賞後にだけ開始し、3列は異なる周期で変動する。演出中には先行して第一・第二図柄を停止できるが、抽選結果は演出や停止順で変更しない。画面には結果そのものを予告せず、入賞IDと`VIRTUAL RNG LOCKED`の状態だけを表示する。

| award | 図柄・演出 | 次状態 |
| --- | --- | --- |
| `miss` | 通常停止または近接リーチ失敗 | 次保留または待機 |
| `smallWin` | 低頻度の同図柄・短い光演出 | 仮想BALL付与後、次保留または待機 |
| `chance` | 近接図柄、神楽閃光、強いリーチ | 失敗または大当たり表示へ |
| `jackpot` | 3図柄揃い、金・朱の全画面演出 | アタッカー、3R、仮想BALL付与 |

## 5. 右打ち・電チュー・アタッカー

通常時の左球道とは別に、右打ちは電チュー支援または大当たり中だけ選択できる。右打ち球は右盤面を通り、電チューが開いているときだけ右保留へ入る。右保留は左保留と別FIFOとして扱い、RELAY中は右保留を優先して消化する。

アタッカーが開いている間は、電チューを経由した右打ち球を**アタッカー誘導球**へ変換する。誘導球は右球道から下部アタッカーへ連続移動し、アタッカーの捕捉領域に入った場合だけ仮想BALLを払い出す。ラウンド中に自動生成される球も同じ誘導球道を通すため、払い出しはLCDの演出だけで発生しない。

## 6. 大当たりとアタッカー

大当たり時はLCD表示だけで完結せず、3Rのアタッカー区間へ移行する。各ラウンドではアタッカーが開き、短時間だけ右打ち球が右球道から誘導される。その入賞数に応じて仮想BALLを付与する。ラウンド終了後は次ラウンドへ、全ラウンド終了後はKAGURA RELAYへ移る。

## 7. KAGURA RELAY

KAGURA RELAYは大当たり後の5G電サポ区間である。右保留で起きるリーチは、通常の神楽リーチではなく、シアンの中継ノイズ、朱の信号回復、金の解放で進む**RELAYリーチ**とする。右保留の失敗の一部は、先に「信号断。沈黙。」を示す復活待機に入り、その後に朱の復帰演出を経て小当たり相当の`win`へ転換する。結果は保留作成時に固定し、復活は結果の後付け抽選に使用しない。

RELAYの残りGは右保留を1件消化して結末へ到達したときにだけ1減る。残りGを5つの物理的な中継セルとしてLCDと盤面に同時表示し、消化済みセルは信号断、現在セルはシアンの中継、残りセルは朱の待機として表示する。カウントダウンは保留の結果や期待度を示さず、遊技区間の残量だけを示す。

アタッカーには5段の菊花役物を接続する。アタッカーの実入賞数に応じて一枚ずつ展開・発光し、5入賞で全開放する。役物の段階は払い出し済みの球数だけに基づくため、後続の結果抽選を示唆しない。

保留には`silent`、`pulse`、`rune`、`static`の信号外観をランダムに付与する。これは結果生成とは独立に選び、保留が未消化の間だけ盤面・LCDの保留灯に表現する。したがって、プレイヤーは演出差を楽しめるが、保留ごとの当否や期待度を直接読み取れない。

## 8. 短期釘路イベント

通常時とRELAY中には、一定時間だけ釘路が変化する`OPEN`または`PINCH`イベントを発生させる。`OPEN`は中央捕捉への補正を強め、`PINCH`は横流れを強める。イベントは表示用の後付け確率変更ではなく、発射時の遊技球速度と捕捉補正へ入力される。短い朱またはシアンの封印札で残時間を示し、終了時には必ず通常の釘路へ戻す。

## 9. 非実装境界

本仕様は実在機種のロゴ、キャラクター、楽曲、図柄、画面構成、固有スペックを複製しない。娯楽用の仮想BALLのみを扱い、現金、賭け、換金、景品、外部サービスとの金銭連携は扱わない。

## 11. 可動物理ギミック

始動口前と電チュー前には**KAGURA ROTOR**を置く。左ローターは半径`2.6`で通常球の始動口前球道へ、右ローターは半径`2.45`で右打ち球の電チュー前球道へ置く。球が接触した場合に接触法線方向の反発へ横方向の回転キックと上向きの速度補正を加える。これにより球速と進路が変化し、通常球と右打ち球は別の物理軌跡で釘路へ戻る。接触は物理位置に基づき、表示だけの演出にしない。

始動口上には左右一対の**SEAL SHUTTER**を置く。`OPEN`釘路イベント中は羽根の間隔を広げ、中央捕捉に向かう球の物理経路を開く。`PINCH`中は羽根を内側へ寄せ、球を左右へ弾きやすくする。羽根は線分衝突として毎フレーム同じ解決に入り、保留内容や入賞済み抽選を変更しない。KAGURA ROTORとSEAL SHUTTERの接触は、仮想BALLの進路だけに作用する。

## 10. 仮想BALLの球循環

仮想BALLの台帳は、発射元の`upperTray`、戻り球と上皿超過払い出しを受ける`lowerTray`、盤面外へ回収された`outCount`に分ける。既存の`balls`は上皿残量を表す互換表示とする。発射時には上皿からのみ1球を減算し、始動口・電チューへの入賞は保留に変換する。アタッカー入賞、小当たり相当の払い出しは上皿へ優先的に加算し、上皿容量を超える分だけ下皿へ送る。

物理球が下部の戻り導入へ到達した場合は下皿へ加算し、アウト口へ到達した場合は`outCount`へ記録する。下皿から上皿への移送は、明示的な`TRAY TRANSFER`操作でのみ行う。下皿、アウト、移送はすべて仮想BALLの遊技状態であり、金銭的な残高、景品、換金、外部連携を示さない。
