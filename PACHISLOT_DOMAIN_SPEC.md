# GEKIAZU RUSH：遊技基盤ドメイン仕様

> **安全境界**：本仕様のクレジット、BET、払い出し、TRANSFERはすべてブラウザ内の仮想ポイントであり、現金・換金・外部決済・実在機確率を扱わない。

## 成立役と役割

| 内部成立役 | 出目の基本形 | 仮想払い出し | 状態差分 |
| --- | --- | ---: | --- |
| `MISS` | 有効ライン不成立 | 0 | 通常ゲーム継続 |
| `REPLAY` | ARC / ARC / ARC | 0、BETを保持 | 再遊技 |
| `ARC_PAY` | ARC / ARC / BOLT | 1 | TRACEポイント +1 |
| `BOLT_PAY` | BOLT / BOLT / BOLT | 6 | ENERGY +1 |
| `SEAL_CHANCE` | SEAL / SEAL / SEAL | 0 | TRACEポイント +3、CZ抽選 |
| `GATE_CHANCE` | GATE / GATE / GATE | 0 | CZ確定またはRUSH中の継続加算 |
| `ZERO_BONUS` | ZERO / ZERO / ZERO | 18 | OVERLOAD BONUS |

## 回胴と停止制御

各リールは21コマの循環するオリジナル図柄帯を持ち、画面には上・中・下の3コマを表示する。レバーON時に`SpinContext`を作成し、成立役、各リールの開始位置、停止順、停止位置、滑り、ライン結果を1ゲーム内で保持する。STOPはすべて有効であり、最初に押されたものを第一停止として記録する。

停止時は、成立役で必要な図柄が停止可能範囲にある場合だけ、最大4コマのオリジナル滑りで有効ラインへ誘導する。停止可能範囲にない場合は、そのリールは自然停止位置を採用する。これにより「停止位置と内部成立役が整合している」手触りを作るが、実在機の制御表や図柄列は転用しない。

## ラインと精算

初期版の有効ラインは中段1本で固定する。3本の中央図柄が一致した場合だけ入賞役を確定し、払い出しと状態差分を適用する。`REPLAY`は次ゲームのBETを保持し、その他の役は仮想クレジットと`PAYOUT`へ加算する。`TRANSFER`は残高をセッション獲得値へ移す観測専用の操作で、外部へ価値を移転しない。

## 状態の責務

`NORMAL LINK`では小役とTRACEポイントを蓄積する。`TRACE MODE`は前兆強度を示すが、CZはSEAL／GATEなどの**回胴結果**からのみ発火する。`FIREWALL BREAK`、`SYNCHRO RUSH`、`OVERLOAD BONUS`の各区間も、PUSH連打ではなく、BET→LEVER→STOP→ライン判定の反復を主操作にする。PUSHは期待度確認・演出解除・結果ログの補助入力に限定する。
