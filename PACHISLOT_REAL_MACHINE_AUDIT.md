# GEKIAZU RUSH：パチスロ実機再現の全面監査

**作成者：Manus AI**  
**監査対象：GEKIAZU RUSH 現行実装**  
**監査日：2026年8月19日**

## まず訂正すべき前提

「玉が専用の口に入ったときに抽選し、スロットが回る」という工程は、始動口へ遊技球が入賞することで図柄変動を始める**パチンコ**の構造である。パチスロの基本工程ではない。パチスロでは、トークンまたは電子メダルをBETし、スタートレバーで回胴を始動し、停止ボタンで回胴を停止して1ゲームが完了する。[1] [2]

Pioneerの公式説明は、3枚を投入してレバーを下げ、3つのSTOPボタンでリールを停止する流れを1サイクルの遊技として示している。[1] したがって、**レバーや手動STOPを削除するとパチスロ実機への再現度は上がらず、むしろ下がる**。それらを消したい場合は、パチスロではなく「オートプレイの演出ゲーム」または「始動口を持つパチンコ風ゲーム」へ製品定義を変える必要がある。

> パチスロの実機再現を選ぶなら、正しい方向は「レバーとSTOPをなくす」ことではなく、**レバーとSTOPが本当に遊技結果へ因果を持つように作り直す**ことである。

## 監査結論

現行版は、BET、LEVER、3リール、3 STOP、PAYLINE、CREDIT、PAYOUTを画面上に備えており、前回までの改修で「演出ゲーム」から前進している。しかし、根幹の制御はなお**結果が先に決まり、リールと停止操作がその結果を表示するだけ**である。このため、操作部はパチスロの見た目をしていても、遊技の感触は依然として「結果の決まった演出を消化するUI」に近い。

最重要の問題は、`PachislotEngine.createSpinContext()`が内部成立役から各リールの`targetCenters`をあらかじめ決め、`stopReel()`がプレイヤーの停止時刻にかかわらず必ずその位置へ停止させる点である。停止順は記録するが、入賞・取りこぼし・制御分岐の実体にはほぼ使われない。つまり、現行のSTOPは**停止を命令するボタンではあるが、停止位置を選ぶ入力ではない**。

## 実機の基準と現行実装の評価

| 監査層 | 実機再現の基準 | 現行の実装 | 判定 |
| --- | --- | --- | --- |
| 始動構造 | BET後、レバーで1ゲームの回胴を始動する。[1] | SMART CREDIT、MAX BET、LEVERを分離済み。 | **概ね正しい** |
| 回胴停止 | 3つのSTOPで回胴を停止し、1ゲームを完了する。[1] | 任意のSTOPボタンを設けている。 | **外形は正しい** |
| 停止位置 | 押した時刻・停止制御・成立役が停止コマを共同で決める。 | `targetCenters`へ常に停止する。押すタイミングは不使用。 | **重大な欠落** |
| 内部成立役 | レバーON時の成立役を基点に、制御・出目・状態が一貫する。 | `drawInternalFlag()`で成立役を選ぶ。 | **部分的に正しい** |
| リール制御 | 成立役と停止時点から、許容範囲内の引き込み／滑りで停止する。 | 滑り表示はあるが、開始位置から目標位置へ必ず移動する。 | **擬似的で不十分** |
| 出目とライン | 図柄帯、停止位置、有効ライン、役の対応表が入賞を決める。 | 中段1ラインと役別出目を定義。 | **最小限は実装済み** |
| 小役・リプレイ | 小役は払い出し、リプレイは再遊技を形成する。 | ARC/BOLT払い出し、REPLAY状態を実装。 | **概念は正しいが浅い** |
| 払い出し | 入賞結果に応じて残高・アウトが一貫して変わる。スマスロは電子的にクレジットを管理する。[2] | PAY/TOTAL/CREDITを更新するが、`TRANSFER`でCREDITを50へ再補充する。 | **台帳整合性が不足** |
| 状態遷移 | 通常・前兆・CZ・AT等は成立役、ゲーム数、内部状態から進む。 | CZ/RUSHで独自の役抽選はあるが、TRACEからCZへ時間で直行する。 | **演出先行が残る** |
| 実遊技と疑似遊技 | 疑似遊技は演出であり、本当遊技と区別される。[3] | SYNC DROP、演出、自動デモと実遊技の境界が不明瞭。 | **要分離** |
| 筐体操作 | BET、レバー、STOP、PUSH、クレジット、計数は別の役割を持つ。[2] | 役割表示は改善済みだが、中央の大ボタンがBETとPUSHを兼ねる。 | **役割混同が残る** |

## 現行実装で誤っている、または再現を妨げる点

### 1. STOPの停止時刻がゲームに影響しない

現行の`createSpinContext()`は、各リールの停止先を`targetCenters`として先に選ぶ。`stopReel()`は、どの瞬間にSTOPが押されても、その停止先へフレームを置き換える。画面上の`slip`は開始位置と目標位置の差分を表示するだけで、入力のタイミングから計算されていない。

この構造では、停止操作の巧拙、第一停止の選択、図柄の見え方、取りこぼしが発生しない。実機風の回胴制御として必要なのは、各リールが時間に応じて連続移動すること、STOP時点のコマを取得すること、成立役の許容停止範囲にある場合だけ最大滑り量で引き込むこと、範囲外なら押した近傍で停止することである。

### 2. 停止順を表示しているが、制御へ使っていない

現行は`stopOrder`を画面に表示するものの、停止順の違いによって入賞役、取りこぼし、前兆の発生、状態遷移が変わる設計になっていない。`ZERO`が早い停止順で現れた場合の演出差以外は、停止順がほぼ装飾である。

停止順を本物の情報にするためには、第一停止で成立役の候補を絞り、第二停止で取りこぼし可能性または制御分岐を作り、第三停止で有効ラインを確定する必要がある。これは実在機の制御表を写すのではなく、GEKIAZU RUSH独自の図柄・小役対応表として設計するべきである。

### 3. 成立役とCZの入口が二重化している

`SEAL_CHANCE`または`GATE_CHANCE`の出目が成立した後、TRACE演出から時間経過だけでCZへ進む。出目がCZへの入口になっている点は良いが、TRACEの残りゲーム、前兆失敗、CZ抽選、ゲーム数管理が存在しないため、事実上「当たり演出を挟んでCZを見せる」だけになっている。

実機感を出すには、通常ゲームで`TRACE POINT`を蓄積し、規定ポイントまたは特定小役でTRACEへ入り、数ゲームの回胴結果によってCZ当否を決める必要がある。CZに入るかどうかを演出タイマーではなく**別ゲームのレバーON時の成立役**で決めるべきである。

### 4. RUSH継続判定がレバーON以外で再抽選される

`enterJudge()`は、RUSH終了後に`Math.random()`で継続可否を決める。これはレバーONでの内部成立役を基点にする設計と矛盾する。遊技結果をすべてレバーONで決める方針なら、継続可否は最終RUSHゲームの成立役・内部状態・到達ENERGYから導出するか、次ゲームのレバーONで抽選する必要がある。

### 5. 仮想CREDITの台帳がセッション再起動と混ざっている

スマスロでは物理メダルを使わず、クレジットに電子的に枚数が表示・管理される。[2] 現行の`TRANSFER`は、クレジットをセッション値へ移した直後にCREDITを50へ戻す。この動作はデモの遊びやすさには寄与するが、払い出し・計数・次セッションの意味を壊す。

実機再現を優先するなら、`TRANSFER`は残高を0にし、`NEW SESSION`は別の明示操作で初期デモクレジットをロードするべきである。実機のカード／精算を完全再現する必要はないが、**計数と補充は別の状態遷移**として分ける必要がある。

### 6. 物理部品の役割を兼用している

実機のBET、レバー、STOP、演出PUSH、クレジット・計数は別の目的を持つ。[2] 現行の中央大ボタンはBETとPUSHを画面状態で兼用している。そのため、物理的に強調されたボタンほど「何をする部品か」が時間によって変わる。

実機再現では、中央大ボタンを演出専用PUSHとして固定し、BETは左のMAX BET、レバーは右のSTART LEVER、停止は中央の3 STOPへ固定するべきである。BETを目立たせたい場合も、中央PUSHを兼用せず、MAX BET自体を大きくする。

### 7. 実遊技と疑似演出の境界が不明瞭

パチスロサミットONLINEは、疑似遊技・疑似停止を本当遊技ではなく演出として動くリールだと区別している。[3] 現行の`SYNC DROP`、自動デモ、カットインには「この間は実遊技を消費しない」という層がない。演出中に回胴を見せる場合、プレイヤーはそれが停止操作を必要とする実遊技なのか、演出用の擬似回胴なのかを認識できない。

実遊技側には`gameId`、BET、成立役、停止順、入賞、払い出しを残し、演出側には`presentation`として別のタイムラインを持たせるべきである。疑似演出は実遊技の結果を隠すためには使えても、実遊技の抽選やクレジットを消費してはならない。

## 実機再現に必要な設計変更

| 優先度 | 必要な実装 | 完成条件 |
| --- | --- | --- |
| P0 | **連続リール位置モデル** | 各リールが時刻に応じて連続移動し、STOP時点のコマを取得する。 |
| P0 | **停止制御エンジン** | 成立役、停止順、押下時の位置、許容滑りを入力にして停止コマを返す。 |
| P0 | **独立した物理操作部** | MAX BET、START LEVER、L/C/R STOP、演出PUSHを役割固定で配置する。 |
| P0 | **正しいクレジット台帳** | BET、REPLAY、PAYOUT、TRANSFER、NEW SESSIONを別操作・別状態に分離する。 |
| P1 | **小役対応表・停止順法則** | 出目、停止順、取りこぼし、前兆をオリジナル規則として定義する。 |
| P1 | **通常→TRACE→CZのゲーム遷移** | 演出タイマーではなく、複数ゲームの成立役とポイントで進む。 |
| P1 | **RUSHのレバーON決定** | 継続・上乗せ・BONUSをレバーON時のフラグと状態から決定する。 |
| P2 | **実遊技／疑似演出分離** | 演出用リールと実リールの時間軸を分け、ゲームIDを汚さない。 |
| P2 | **サブ表示と設定メニュー** | IN／OUT、直近役、ゲーム数、音量・光量、デモ開始を独立表示する。 |

## 製品判断

この製品を**パチスロ実機再現**として進めるなら、レバーとSTOPは残すべきである。ただし現行のように、押しても決められた結果を表示するだけでは不十分である。次の実装は、見た目の追加ではなく、`SpinContext`を「開始位置・開始時刻・成立役・停止時刻・停止制御結果・入賞結果」を持つ連続シミュレーションへ置換することになる。

一方、始動口へ玉が入り、図柄が自動変動し、ボタンで演出を選ぶ構造がご希望なら、それはパチンコとして別ゲームに作り直すべきである。両者を一つのゲームへ混ぜると、パチスロにもパチンコにも見えない体験になる。

## 参考文献

[1]: https://www.slot-pioneer.co.jp/global/en/howto.html "Pioneer Ltd. Japan’s unique amusement ‘Pachislot’ — How to play"
[2]: https://www.sankyo-fever.jp/research/44/ "SANKYO調査隊：2022年11月導入開始！『スマスロ』を解説"
[3]: https://www.pachislot-summit.com/glossary/ "パチスロサミットONLINE：用語集"
