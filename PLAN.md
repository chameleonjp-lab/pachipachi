# Game Plan: GEKIAZU RUSH — Pachinko Edition

## Risk Tasks

### 1. 遊技球の連続移動と始動口入賞
- **Why isolated:** 見た目だけの玉と、始動口に実際に入る玉が別系統になると、ハンドル操作・盤面・保留・図柄変動の因果が崩れる。
- **Approach:** `PachinkoEngine`が遊技球の座標、速度、ピン反射、落下、始動口検出を純粋な状態更新として扱う。`GameWorld`はハンドル入力中だけ一定間隔で球を発射し、始動口へ到達した球だけを保留へ変換する。
- **Verify:** ハンドルを離すと発射が止まり、発射済みの球だけが盤面を最後まで移動する。始動口に入った球だけが保留灯を増やし、保留上限時には新たな始動抽選が追加されない。

### 2. 保留キューと図柄変動の因果
- **Why isolated:** 変動結果を演出タイマーで後から乱数化すると、保留の意味がなくなり、途中操作で二重変動が起きる危険がある。
- **Approach:** 始動口入賞の瞬間に`VariationContext`を生成してFIFOの保留キューへ格納する。液晶が待機中なら先頭保留だけを消費し、通常変動→予告→リーチ→停止／大当たりへ一方向に進める。
- **Verify:** 複数入賞時に保留灯が順番に減り、各変動が一回だけ開始・終了する。図柄停止操作は存在せず、リーチは事前に決まった変動文脈の表示結果として発火する。

### 3. 大当たり中のアタッカーとラウンド
- **Why isolated:** 大当たり結果だけを画面に出して終了すると、パチンコの主たる遊技区間であるアタッカー開放とラウンド進行が欠落する。
- **Approach:** 大当たりは`jackpot`状態を経由し、固定ラウンドのアタッカー開放、球入賞、仮想球払い出し、ラウンド間幕、終了を明示的な状態として進行させる。払い出しは娯楽用の仮想BALLのみであり、現金・換金・景品とは接続しない。
- **Verify:** 大当たり時だけアタッカーが開き、ラウンド表示が進み、ラウンドごとの仮想払い出しがBALLカウンタへ反映される。終了後に通常時または保留消化へ戻る。

### 4. ReactとBabylonの二重マウント
- **Why isolated:** 開発時のStrictModeで描画エンジンが二重生成されると、描画ループ、球の視覚表現、イベントが重複する。
- **Approach:** `GameCanvas`のrefで生成を一回に制限し、アンマウント時に描画ループ、シーン、DOMイベント、WebGLエンジンを順に解放する。
- **Verify:** ページ再読み込みと開発時の更新後もキャンバスは一枚だけで表示され、コンソールにWebGL・dispose関連のエラーが出ない。

## Main Build

- 既存のBET、レバー、3回胴、STOP、ペイライン、CZ、RUSHを撤去する。
- オリジナルのネオン神楽回路盤面に、右側ハンドル、遊技球、始動口、保留4灯、LCD図柄変動、リーチ、電チュー、アタッカー、ラウンド、仮想BALL払い出しを導入する。
- ハンドルはポインタ長押し、マウス操作、タッチ、Spaceキーで操作でき、入力強度は盤面上の発射間隔に反映する。
- 変動結果は始動口入賞時に確定し、通常変動、前兆、リーチ、ハズレ、当たり、大当たりの表示を一貫した状態遷移で行う。
- `?demo`では通常入賞、リーチ、ハズレ、大当たり、アタッカーの流れを固定順で見せる。
- **Assets needed:** 16:9の完成参照、3:4盤面テクスチャ、始動口、アタッカー、透明な遊技球。
- **Verify:**
  - ハンドル入力と遊技球の発射が一致する。
  - 始動口入賞だけが保留・図柄変動を開始する。
  - 保留、LCD、アタッカー、ラウンド、仮想BALLカウンタが読みやすい。
  - モバイルでは長押し・タッチ操作で同じ因果を体験できる。
  - 生成素材が欠損せず、画面にプレースホルダー、レイアウト崩れ、既存機種の固有要素がない。
  - コンソールエラーがなく、色、密度、正面筐体の構図が新しいビジュアルターゲットと整合する。
